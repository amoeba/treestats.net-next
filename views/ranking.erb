<h2><%= @ranking_name %></h2>

<%= erb :_page_controls %>

<table class="listing">
  <thead>
    <tr>
      <th>Server</th>
      <th>Character</th>
      <th><%= @ranking_name %></th>
    </tr>
  </thead>
  <tbody>
  <% @characters.each do |character| %>
    <tr>
      <td><a href="/<%= character[:server] %>"><%= character[:server] %></a></td>
      <td><a href="/<%= character[:server] %>/<%= character[:name] %>"><%= character[:name] %></a></td>
    </tr>
  <% end %>
  </tbody>
</table>
        <td><%= @formatter.call(character[@value_col]) %></td>

<%= erb :_page_controls %>
